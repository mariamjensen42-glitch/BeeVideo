# BeeVideo

BeeVideo 是一个可插拔的播放器外壳：App 自身不持有、不分发、不推荐任何内容，
只提供「内容源解析 + 视频播放」的管道。所有内容来源由用户自行配置。

## Language

**内容源（Content Source）**:
用户自行配置的一份来源清单，装载后产生若干**站点**。
App 不内置、不推荐、不分发任何内容源。
_Avoid_: 配置、订阅、源列表、config

**站点（Site）**:
内容源里的一个可查询站点，有独立标识，可标记为「可搜索」。
同一份内容源里，同一个爬虫类被配成十几个站点是常态。
_Avoid_: 源、接口、渠道、api

**线路（Play Line）**:
同一部影片的一条可选播放来源。
只有同一条线路内的**剧集**编号才可比 —— 换线路后集号不作数。
_Avoid_: 源、播放源、flag

**剧集（Episode）**:
线路内的一个可播放单元。
它的播放标识**不一定是地址** —— 可能是一个待站点兑换的内部 id。
_Avoid_: 分集、集数、分片

**播放目标（Play Target）**:
一次播放真正需要的全部东西：地址，以及**必需的请求头**。
两者必须同时到手才有意义 —— 拆成两次调用就会出现「拿到地址、换集时头没跟上」。
地址也可能是**需要站外解析服务的网页**，本项目不内置那类服务，只会如实说明。
_Avoid_: 播放地址、直链、url、链接

**观看进度（Play Progress）**:
用户在一部影片上「上次看到哪」。
一部影片一条，按**线路 + 剧集**定位 —— 两者任一不符即视为没看过。
_Avoid_: 历史、记录、history、断点

**收藏（Keep）**:
用户主动留下的一部影片，连同留下时的快照字段。
快照是数据本身而非冗余：它必须在来源不可用时仍能显示。
_Avoid_: 书签、标记、like

**播放会话（Playback Session）**:
一次连续的播放：从起播到停止，期间位置连续、状态可观察。
关键性质是它**活得不比它所在的页面短** —— 界面重建不打断它。
_Avoid_: 播放器、player、内核、engine

**配置会话（Source Session）**:
一次「内容源已装载」的有效期。装载、替换、清除都发生在某个会话之内；
用户换用或清除内容源时，前一个会话结束。
_Avoid_: 配置管理、源管理、session 管理器

**播放设置（Playback Settings）**:
用户对「看过的内容要不要留在本地」的选择：一个开关，加一个占用上限。
它是这个 App 唯一会持续占用用户存储的地方。
_Avoid_: 缓存设置、播放器配置、preferences

**媒体缓存（Media Cache）**:
把看过的内容留在本地磁盘的那份副本。
⚠️ **清空它要求没有播放器正在播放** —— 那会释放正在被读取的缓存实例。
_Avoid_: 缓存、离线缓存、cache、磁盘缓存

**外观三态（Theme Mode）**:
用户对深浅色的选择：跟随系统 / 浅色 / 深色。
**三态而不是布尔**是有意的 —— 「跟随系统」是一个可以一直选着的选项，不是一个初始值。
_Avoid_: 主题开关、深色模式标志、darkTheme 布尔
