# 2026-09-16

## 记忆文件分层（起因：MEMORY.md 被截断）

`MEMORY.md` 涨到 5600 字符 / 7483 字节，**注入时被截断** —— 等于自己的约定有一半收不到。
本次拆成两层：

- `MEMORY.md` → 只留仍生效的**硬约束**，一条一句（现 3429 字符 / 5.2KB）
- `REFERENCE.md`（新）→ 做具体子系统时才需要的深层细节：
  CatVod/MacCMS 全文、Hero 算式与踩坑、M3 Expressive 论证、工具链踩坑、真配置格式普查数据

以后新增内容只往 `MEMORY.md` 加**一句话规则**；论证、实测数据、踩坑过程写进日志或 `REFERENCE.md`。

## 真机验证：`ext` 的四种形态（全绿）

对照语料 `github.com/qist/tvbox`（11343 stars，~40 个根 JSON）的格式普查结果补全了 mock，
逐个在 Redmi K40 上跑通：

| ext 形态 | chip 显示 | 宿主是否下载 |
|---|---|---|
| 缺失 | `ext=空` | — |
| inline 对象 | `ext[73]:{"url":"http…` | — |
| `http` 地址 | `ext[56]:{"class": …` | ✅ 先 GET 再传内容 |
| 复合 `$$$` | `ext[96]:./lib/token…` | ❌ 正确地**不**下载 |

- 复合形态最值得记：样本形如
  `./lib/token.json$$$http://…$$$noproxy$$$1$$$./json/wogg.json$$$MOGG`，
  **不以 http 开头**，所以按参照实现（`Site.fetchExt` 只判 `startsWith("http")`）**不能下载**，
  必须原样透传。真机实测 **96 字符逐字节**到达爬虫 `init`；服务端日志里**没有** `/ext/` 请求。
- `Mock Jar 源` 的 chip 显示 `ext=空` —— 这是 `MockSite` 里那个**无条件** marker 的作用：
  它能区分「init 根本没被调用」和「marker 被解析器吃了」两件看起来一样的事。
  顺带证明 `init(Context, String)` 签名改动无回归。
- 服务端日志还抓到 `ac=detail&ids=3` —— MacCMS **封面回填**那条路在真实运行。

## 真机验证：异常配置的报错文案（全部可读、可行动）

| 配置形态 | 屏幕上实际显示 |
|---|---|
| `{"urls":[…]}` | 这是一个配置合集（内含 urls 列表），不是单个配置。请改成填其中一个具体的配置地址。 |
| `{"msg":"…"}` | 配置地址返回了提示：该配置已停止维护，请更换 |
| `{"spider":""}` | 这个配置里没有任何可用站点（缺少 sites，或每个站点都没有 key）。 |

JS / Python 引擎的拒绝文案也确认了（此前只看到空白页，靠推断）：

- `读取失败：站点「Mock JS 源」是 JS 爬虫（api = ./js/某个爬虫.js），需要 JS 引擎，当前版本不支持。`
- `读取失败：站点「Mock Py 源」是 Python 爬虫（api = ./py/某个爬虫.py），需要 Python 运行时，当前版本不支持。`

## 工具修复（两个都是真 bug，不是使用姿势问题）

1. **`ui_pick.py` 的 `--list` 永远不工作**：`[a for a in argv if not a.startswith("--")]`
   把 `--list` 自己也过滤掉了 → 位置参数为空 → 直接打印帮助并返回 1。
   同一行还会把 `--row 1367` 里的 `1367` 当成目标文本。
   改成显式剔除「带值选项 + 它的值」。
2. **`ui_pick.py` 只朝一个方向滑动**：横向 chip 行的**滚动位置会保留** ——
   上次停在末尾的话，再进来往左滑永远是终点，前面 5 项一辈子找不到。
   症状极具迷惑性：连滑 8 次「没找到 Mock Jar 源」，`当前可见` 也没有它，
   看上去像文案写错了；实际上它就在**左边屏外**（当时可见的正是第 6/7/8 项）。
   改成**先按指定方向找、找不到自动换反方向再找一遍**，并新增 `--right`。

新增 `ui_setfield.py`：按 **class 找 EditText**（不按文本 —— 输入框内容是「当前值」，你没法预知），
点进去 → MOVE_END → 按旧值长度连发 DEL 清空 → `input text` 新值。

踩坑记录：
- 用 `ui_pick.py` 传**部分文本**去点输入框会精确匹配失败 → 字段没聚焦 →
  新值被**插到旧值前面**，拼成 `...depot.jsonhttp://...config.json`（看着像"没清空"）。
- `netstat` 输出是 GBK，Python 读要 `encoding='gbk'`，否则 `UnicodeDecodeError` 且 stdout 变成 `None`。

## 单元测试

新增 `CatVodConfigParserTest`（27 个用例，实测 27 passed / 0 failed），把上述格式兼容规则钉死：三种非法形态、
`api` 随 `type` 变（类名 vs URL 模板）、四种 `ext` 形态、`spider` 的 `url;md5;hash` 只有第一段是路径、
缺 `type` 退化成 JSON、`name` 回退成 `key`、空 `msg` 不算提示、`urls`+`sites` 并存时按配置解析。

⚠️ 踩坑：android.jar 里的 `org.json` 是**桩**，JVM 单测一调 `JSONObject` 就抛 `Stub!`
→ 必须加 `testImplementation("org.json:json:20240303")`（已进 `libs.versions.toml` 的 `orgJson`）。
**只进 testImplementation，不进 APK。**

## 清理

删除本次临时目录：
- `_ref/` —— **从 CatVodTVSpider 下载的权威源码，GPL-3.0，绝不能留在仓库里**（25KB）
- `_cfgcache/`（真实配置缓存，469KB）、`_jarcls/`（javac 临时输出）、`_shot/`

证据截图归档到 `docs/screenshots/23-source-compound-ext.png`。

## CatVod 兼容层：照 `FongMi/TV` 权威源码重译，并把 ABI 钉死

起因：`FongMi/TV` 才是 Java 版空壳的**原始实现**，`com.github.catvod.*` 应当照它翻译，
而不是从下游衍生版推测。

### 怎么拿到权威源码
`raw.githubusercontent.com` 直连会断连，走 `gh api repos/FongMi/TV/contents/<path>?ref=fongmi`
拿 base64 再解码（脚本 `fetch_fongmi.py`，`--list <prefix>` 查 `tree.json`）。
⚠️ **默认分支是 `fongmi` 不是 `main`** —— 用 `main` 拿不到 tree。
❗抽出来的是 **GPL-3.0** 源码，只作行为规格读，**用完即删**（本轮已删 `_fongmi/`）。

### 读源码 + 实测 jar 后改掉的 8 处
| 问题 | 依据 |
|---|---|
| `client()`/`safeDns()` 写成**实例方法** | 实测 `invoke-static` —— **本来永远不可能工作**，纯静默 bug |
| 方法名写成 `proxyLocal` | 权威是 `proxy(Map)` → 覆写根本没生效 |
| 搜索第一页调了**三参版** | 权威 `hasPage = !page.equals("1")`，第一页走**两参版** |
| 漏了 `homeVideoContent()` 的调用与合并 | 权威调**两次**，非空则 `result.setList(...)` 覆盖 |
| 漏了 `siteKey = key` **先于** `init` | `JarLoader.getSpider` 的顺序 |
| 缺 `SpiderApi` / `Notify` 两个宿主类 | jar 会引用 |
| 缺 `Init.init(Context)` 调用点 | `JarLoader.load` 建完 DexClassLoader 就调 |
| `newCall` 返回**已执行的 `Response`** | 权威返回 `Call`，返回类型本身就是描述符的一部分 |

另外：`Spider` 默认返回 `""`（不是 `"{}"`）；`siteKey` 改 `@JvmField`；`SpiderApi` 必须是
**class 不是 interface**；`init` 只留 `(Context)`/`(Context,String)`（删掉自造的 `init(String)`）。

### 工具链：新增 `dex_probe.py`
真实 jar 是预编译的，**签名就是 ABI**。写了个真正的 DEX 解析器读 `method_ids`/`field_ids`，
再减去 jar 自身 `class_defs`，得到「这个 jar 向宿主索要什么」。踩坑三条：
- `dexdump` **不能 mmap Windows 上的 jar**，要先用 `zipfile` 抽出 `classes*.dex`
- 正则扫 dexdump 会因混淆类刷出 **4186 条假 field ref**
- `*_upgraded.jar` 的 `method_idx_diff` **不连续**，手写 `class_data_item` 解析必崩
  → 扫 `method_ids` 才可靠

### `build_mock_jar.py` 修了 3 个真 bug
1. `subprocess.run(text=True)` 用 UTF-8 解码 javac/d8 的 **GBK** 输出 → 先炸 `UnicodeDecodeError`，
   真错误看不见。
2. `fail("javac 失败\n" + r.stdout + r.stderr)` 里 `stdout`/`stderr` 可能是 `None` → `TypeError`。
   现在统一走一个 `run()` 助手：GBK 解码 + `or ""` 兜底 + 失败时把两路输出分块打全。
3. **第三方 jar 没进 classpath**：`Spider.client()` 返回 `okhttp3.OkHttpClient`，宿主 classes 里没有
   → javac 报「找不到okhttp3.OkHttpClient的类文件」。现在按坐标从 Gradle 缓存取
   okhttp 4.12.0 + okio 3.6.0，同时喂 javac `-cp` 与 d8 `--lib`（**只作库、不 dex 进 jar**，
   否则 jar 里会再定义一份 `Spider`，`instanceof` 直接失败）。

### 装机实测：mock jar 发两条标记分类，全绿
`mock_spider.jar`（3393 字节，md5 `a65b46d3…`）重写为逐条「证明」上述每处修复：

| UI 上看到 | 证明了 |
|---|---|
| `ext[96]:./lib/token.…` | ext 原样透传（96 字符、首 12 字逐字对得上） |
| `key=mock_jar_ext_compound 静态=ok api=有` | siteKey 在 `init` 前赋值 / 三个 static 可解析 / `initApi` 收到非 null |
| 精选与网格出现「推荐位专用」 | `homeVideoContent()` 被调，且覆盖了 list |
| 切到 `Mock Jar 源` 得 `key=mock_spider` + `ext=空` | **null ext 不崩** —— Kotlin 给非空参数插 `checkNotNullParameter`，写成 `String` 就 NPE |
| 点「电影」→ 8 部、精选 `01/05` | `categoryContent` 走通 jar → OkHttp → mock → 解析 → UI |

`logcat` 里**没有** `NoSuchMethodError`/`NoSuchFieldError`/`IncompatibleClassChangeError`/
`IllegalAccessError`，也没有 `FATAL`；缓存文件名 = 新 jar 的 md5，说明**没命中旧缓存**。

⚠️ 中间踩到：旧 mock 服务进程还占着 18080，而 md5 是**服务启动时**算的 → 必须 `taskkill` 重启，
否则 App 会继续跑上一版 jar，且**毫无报错**。

`javac` 这一步本身就是最强静态证明：`@Override`（签名）+ 直接写 `this.siteKey`（字段可见性）+
写 `Spider.client()`（静态性）三者一起被编译器校验，**javac 一过就等于全对**。

### 顺带发现（不是 bug，是权威行为）
`homeVideoContent` 非空会**覆盖** `homeContent` 的 list，而首页「推荐」分类读的正是这个 list
（`listByCategory(RECOMMEND)` → `home.featured`）。所以 mock 只回 1 条推荐时，
首页网格就从 15 条塌成 1 条 —— 这是权威 `result.setList(list)` 的忠实结果，属预期。

### 单测
新增 `JarSiteClientTest`（7 个用例）：搜索只调**两参**版（三参版调用次数断言为 0）、
空关键词不碰 spider、`homeVideoContent` 覆盖 featured、空 `homeVideoContent` 保留原 list、
抛异常不影响首页、基类默认值 `""` 解析为空、`siteKey` 可赋值。
⚠️ 一开始 4 个用例失败（`expected:<h1> but was:<test_key:h1>`）—— **是用例写错了**，
id 本来就带站点命名空间，加 `ns()` 助手后全绿。
⚠️ 单测还需 `unitTests.isReturnDefaultValues = true`，否则 `android.util.Log` 抛 "Stub!"。

`gradle :app:assembleDebug` **BUILD SUCCESSFUL**，单测 **35/35 通过**。

### 清理与记忆维护
删除 `_jars/`（7 个真实第三方 jar + dexdump 反汇编，**294 MB**）、`_fongmi/`（GPL-3.0 源码快照）、
`_ui*.xml`。两张装机证据归档到 `docs/screenshots/catvod-abi-marker-compound-ext.png` /
`catvod-abi-noext-category.png`。

`MEMORY.md` 又被截断（4128 字符 > 3000），本轮压到 **2955 字符**；ABI 校验方法、opcode 真值表、
工具链补充全部下沉到 `REFERENCE.md`（现 10047 字符）。

### 尚未决断
真实 jar 的其余第三方依赖还没加：**slf4j 实测 7/7 命中**（只加 Gson 是不够的），
rxhttp / `com.whl` / grizzlylabs / zxing / X5 / okio 部分命中。当时的顾虑是影响面，
但 slf4j 的命中率与 Gson 同级 —— 值得补。

---

## 首次出 release 包（MVP）

已接入真实源，转入发版。发版前仓库里只有 `assembleDebug` 的用法，release 侧**完全没有配置**：
没有 keystore、没有 `signingConfig`、`optimization.enable = false`、没有 keep 规则。

### 签名
- `keytool` 生成 `keys/beevideo-release.jks`（alias `beevideo`，PKCS12，RSA 2048，10000 天）。
  口令字符集刻意去掉 `0O1lI`，避免手抄看错。
- 四项写进 `local.properties` 的 `beevideo.keystore.*`；`.gitignore` 补了 `keys/`、`*.jks`、`*.keystore`。
- ⚠️ **没有 `signingConfig` 时 `assembleRelease` 出的是未签名 APK**，Gradle 只打一行不显眼的 warning，
  很容易误以为"构建成功就能装"。配了 `validateSigningRelease` 才是真的。

### R8：两种破坏方式要分开堵
这里的关键认知是 —— **混淆和删除是 R8 的两件独立的事**，keep 规则管不了改名，
`-dontobfuscate` 也管不了删除：

| 手段 | 挡住的 |
|---|---|
| `-keep class com.github.catvod.** { *; }` | 成员**被删** —— 那些方法只给外部 jar 调，本 App 用不到，R8 眼里就是死代码 |
| `-dontobfuscate` | 名字**被改** —— jar 字节码里写死 `superclass = Lcom/github/catvod/crawler/Spider;` |

少任何一条，症状都是「编译过、装得上、只有那个站点被打开时才炸」。
关混淆对 MVP 几乎没有代价（本 App 没有要保护的 IP，也没有签名校验），
但把最致命的一整类风险直接消掉了。

另外 `-keepattributes Signature` 必须留：R8 默认删它，删了之后 Gson 拿不到泛型，
症状是「能请求、能返回、解析出来全是空对象」。

### 用差分代替期望清单
新写 `.workbuddy/scripts/verify_release.py`：拿 release 产物和 **debug** 产物比 catvod 接口面，
**差集必须为空**。debug 是未优化编译的，天然拥有完整接口面 —— 所以这个判据不需要维护，
也不会因为"忘了往清单里加一项"而放行一个静默故障。比手写期望清单可靠得多。

结果：差集为空，`Spider` 的 13 项点名（`proxy`/`liveContent`/`action`/`categoryContent`/
`playerContent`/`initApi`/`client`/`safeDns`/`homeVideoContent`/`#siteKey` …）全过。

### `dex_probe.py` 修掉一个真 bug（顺带解释了老疑案）
`method_idx_diff` / `field_idx_diff` 是**组内**增量：DEX 把方法分成 direct 与 virtual、
字段分成 static 与 instance **两个各自独立排序的组**，每组第一条 diff 都相对 0。
原来跨组连续累加，第二组的 index 会飘到表外 → `struct.error: unpack_from requires a buffer of …`。

- **只在同时拥有两组的类上炸**，所以简单 jar 上一切正常
- 之前 `*_upgraded.jar` 解析失败被我判断成「那些 jar 格式特殊」—— **判断错了**，是解析器的错。
  这次在 R8 产物上再次触发才定位到根因。修法就是两组分别 `idx = 0`。
- 同一个 bug 在 `verify_release.py` 的字段解析里也有，一并修了（`siteKey` 一开始报"缺失"就是这么来的）。

### 体积
**5.16 MB**（debug 75.46 MB，压掉 93.2%）：classes.dex 4.58 MB + resources.arsc 0.35 MB + res 54 KB。
release 只有 **1 个 dex**（debug 17 个）。大头是 R8 去死代码 + 资源收缩，
以及 `material-icons-extended` 里 99% 的图标被删掉。

### 虚惊一场：release 里找不到 `ic_launcher`
grep 图标条目返回 0，badging 里也没看到 `application-icon` —— 差点当成"图标被削掉了"。

实际是 **AGP 9 的资源名混淆**（`optimizeReleaseResources` 任务）：`res/E4.xml`、`res/-6.webp` 就是它们。
资源 **ID 稳定**，所以 `R.string.x` 与清单里的 `@mipmap/ic_launcher` 都不受影响；
`aapt2 dump badging` 里 `application-icon-160:'res/E4.xml'` 与 `launchable-activity` 都在。
- 教训：**别拿资源文件名当存在性判据**，要看 ID 或 badging。
- 真正需要警惕的是**按名字查资源**（`getIdentifier`）—— 已 grep 确认当前代码没有这种用法。

### 两个坑记一下
- `optimization.enable = true` 会**连带开启资源收缩与资源改名**（`optimizeReleaseResources`、
  `convertShrunkResourcesToBinaryRelease` 会出现）。
- AGP 9 对 minSdk ≥ 24 默认关掉 v1 签名 → 产物里**没有 `META-INF/*.RSA`**。
  「META-INF 里没有证书」**不等于未签名**，要看 v2/v3 签名块。
- `keepRules.files` 在 9.3.2 里已废弃（提示改用 keepRules source folder），
  但**没有为消 warning 去猜目录名** —— 猜错等于规则静默不加载，那是最致命的失败模式。

### 发版清单状态
- ✅ release APK 已出、已签名、catvod 接口面全项通过、混淆已关、图标与入口正常
- ⚠️ **release 与 debug 签名不同** → 从 debug 换装 release 必须先卸载，**App 内已配的源会全丢**
- ⚠️ 仓库**一个 commit 都没有**（全在 `master` 的未跟踪状态），发版前至少要提交并打 tag
- 未动：版本号仍是 `versionCode 1 / versionName "1.0"`（首个 MVP 版本，合理）
- 未动：`.workbuddy/scripts/` 下的 mock 服务与探测脚本仍在（不进 APK，但要不要留待定）
